---
interface Props {
  rating: number;
  max?: number;
  class?: string;
  style?: string;
}

const { rating, max = 5, ...rest } = Astro.props;
---

<div {...rest}>
  {
    [...Array(max)].map((_, i) => (
      <span class={`star ${i < rating ? "checked" : ""}`}>★</span>
    ))
  }
</div>

<style>
  .star {
    color: #ccc;

    &.checked {
      color: #ffd700;
    }
  }
</style>

