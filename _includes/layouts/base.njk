<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ (title + " - " + metadata.title) if title else metadata.title }}</title>
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ description or metadata.description }}"></description>

    <link rel="icon" href="/img/favicon.svg">

    <style>
        {% scss 'scss/index.scss' %}
    </style>
</head>
<body>
    {% include "header.njk" %}

    <main>
        {{ content | safe }}
    </main>
</body>
</html>