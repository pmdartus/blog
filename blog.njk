---
title: Blog
description: Random posts about Pierre-Marie Dartus' journey into programming.
layout: layouts/base.njk
eleventyNavigation:
  key: Blog
  order: 1
---

<section class="post-list">
    <h1>Blog posts</h1>

    {% for post in collections.posts | reverse %}
    <article>
        <a href="{{ post.url }}">
            <h2>{{ post.data.title }}</h2>
        </a>

        <aside>
            <time datetime="{{ post.data.date | formatdate('yyyy-MM-dd') }}">{{ post.data.date | formatdate("MMM. dd, yyyy") }}</time>
        </aside>

        <p>{{ post.data.description }}</p>
    </article>
    {% endfor %}
</section>